# TASK 1

- There is an issue with the .json that is affecting the downstream process which is an App/UI that displays (ie renders) all the info in a user/doctor friendly way.
- However, because the .json files do not have a specific structure, the App/UI is having some trouble
- So the goal of this task is to look at the .json files and come up with a uniform structure for the .json file that can handle all the gemini transcripts and is consistent for every patient. So, it should be logical and essentially a superset.
- Once we come up with a unifying framework for the .json format, we will make a modification to the SKILL so that when it reads the gemini transcript, it utilizes that uniform framework. But that will be in the next step.

